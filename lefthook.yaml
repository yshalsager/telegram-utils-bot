pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.py"
      run: mise x uv -- uv run ruff check --fix {staged_files}
      stage_fixed: true

    fmt:
      glob: "*.py"
      run: mise x uv -- uv run ruff format {staged_files}
      stage_fixed: true

    ty:
      glob: "*.py"
      run: mise x uv -- uv run ty check src --output-format concise
